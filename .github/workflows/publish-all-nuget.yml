name: Publish All NuGet Packages

# Cascading publish of ALL NuGet packages:
# 0.  Summarizer.Core (pure interfaces) → wait
# 0.5 CodeSummarizer, Storage.Core, DataSummarizer.Core (parallel) → wait
# 1.  DocSummarizer (interfaces + services) → wait
# 2.  Backends (Rdbms.Sqlite, FullText.Lucene, FullText.Sqlite, Anthropic, OpenAI) parallel → wait
# 2.5 LLM, Decomposer, ImageSummarizer, AudioSummarizer (parallel) → wait
# 2.5c VideoSummarizer (needs Image + Audio) → wait
# 3.  DoomSummarizer.Core → wait
# 4.  Sources + Readers + Plugins (all parallel) → wait
# 4.5 Plugins.Complete (meta-package)
# 5.  DoomSummarizer.Complete (meta-package: fat CLI with everything)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for ALL packages (e.g., 1.0.0)'
        required: true
        type: string

permissions:
  id-token: write
  contents: write
  packages: write

env:
  DOTNET_VERSION: '10.0.x'
  DOTNET_QUALITY: 'preview'

jobs:
  # ── Tier 0: Summarizer.Core (pure interfaces) ──────────────────────────
  publish-summarizer-core:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ github.event.inputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Create version tag for MinVer
        run: |
          TAG="sumcorev${{ github.event.inputs.version }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists, skipping"
          else
            git tag "$TAG"
          fi

      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizer.Core/Mostlylucid.Summarizer.Core.csproj
          dotnet pack src/Mostlylucid.Summarizer.Core/Mostlylucid.Summarizer.Core.csproj \
            -c Release -o ./artifacts

      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with:
          user: 'mostlylucid'

      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          dotnet nuget push ./artifacts/*.snupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --skip-duplicate
        continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: summarizer-core-pkg
          path: ./artifacts/*

  # ── Wait for Summarizer.Core on NuGet ──────────────────────────────────
  wait-for-summarizer-core:
    needs: publish-summarizer-core
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              "https://api.nuget.org/v3-flatcontainer/mostlylucid.lucidrag.summarizer.core/${VERSION}/mostlylucid.lucidrag.summarizer.core.${VERSION}.nupkg")
            echo "Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then exit 0; fi
            sleep 30
          done
          echo "Timeout — continuing anyway"

  # ── Tier 0.5: CodeSummarizer.Core ─────────────────────────────────────
  publish-codesummarizer:
    needs: [publish-summarizer-core, wait-for-summarizer-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Create version tag for MinVer
        run: |
          TAG="codesumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true

      - name: Build & Pack
        run: |
          dotnet restore src/CodeSummarizer.Core/CodeSummarizer.Core.csproj
          dotnet build src/CodeSummarizer.Core/CodeSummarizer.Core.csproj -c Release -p:GeneratePackageOnBuild=false
          dotnet pack src/CodeSummarizer.Core/CodeSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build

      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with:
          user: 'mostlylucid'

      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          dotnet nuget push ./artifacts/*.snupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --skip-duplicate
        continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: codesummarizer-pkg
          path: ./artifacts/*

  # ── Wait for CodeSummarizer ──────────────────────────────────────────
  wait-for-codesummarizer:
    needs: [publish-summarizer-core, publish-codesummarizer]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              "https://api.nuget.org/v3-flatcontainer/mostlylucid.lucidrag.codesummarizer/${VERSION}/mostlylucid.lucidrag.codesummarizer.${VERSION}.nupkg")
            echo "Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then exit 0; fi
            sleep 30
          done
          echo "Timeout — continuing anyway"

  # ── Tier 0.5b: Storage.Core + DataSummarizer.Core (parallel) ──────────
  publish-storage-core:
    needs: [publish-summarizer-core, wait-for-summarizer-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="storcorev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Storage.Core/Mostlylucid.Storage.Core.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/Mostlylucid.Storage.Core/Mostlylucid.Storage.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Storage.Core/Mostlylucid.Storage.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: storage-core-pkg, path: './artifacts/*' }

  publish-datasummarizer-core:
    needs: [publish-summarizer-core, wait-for-summarizer-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="datasumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/DataSummarizer.Core/DataSummarizer.Core.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/DataSummarizer.Core/DataSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/DataSummarizer.Core/DataSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: datasummarizer-core-pkg, path: './artifacts/*' }

  # ── Wait for Storage + Data ─────────────────────────────────────────
  wait-for-storage-data:
    needs: [publish-summarizer-core, publish-storage-core, publish-datasummarizer-core]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet for Storage + Data
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for pkg in mostlylucid.lucidrag.storage.core mostlylucid.lucidrag.datasummarizer; do
            echo "Waiting for $pkg ${VERSION}..."
            for i in $(seq 1 30); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                "https://api.nuget.org/v3-flatcontainer/$pkg/${VERSION}/$pkg.${VERSION}.nupkg")
              echo "  Attempt $i: HTTP $STATUS"
              if [ "$STATUS" = "200" ]; then break; fi
              sleep 30
            done
          done

  # ── Tier 1: DocSummarizer ──────────────────────────────────────────────
  publish-docsummarizer:
    needs: [publish-summarizer-core, wait-for-summarizer-core, wait-for-codesummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Create version tag for MinVer
        run: |
          TAG="docsumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true

      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.Core/Mostlylucid.DocSummarizer.Core.csproj \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/Mostlylucid.DocSummarizer.Core/Mostlylucid.DocSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.Core/Mostlylucid.DocSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}

      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with:
          user: 'mostlylucid'

      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          dotnet nuget push ./artifacts/*.snupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --skip-duplicate
        continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: docsummarizer-pkg
          path: ./artifacts/*

  # ── Wait for DocSummarizer ─────────────────────────────────────────────
  wait-for-docsummarizer:
    needs: [publish-summarizer-core, publish-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              "https://api.nuget.org/v3-flatcontainer/mostlylucid.lucidrag.docsummarizer/${VERSION}/mostlylucid.lucidrag.docsummarizer.${VERSION}.nupkg")
            echo "Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then exit 0; fi
            sleep 30
          done
          echo "Timeout — continuing anyway"

  # ── Tier 2: Backend packages (parallel) ────────────────────────────────
  publish-rdbms-sqlite:
    needs: [publish-summarizer-core, wait-for-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="dssqlitev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.Rdbms.Sqlite/Mostlylucid.DocSummarizer.Rdbms.Sqlite.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.Rdbms.Sqlite/Mostlylucid.DocSummarizer.Rdbms.Sqlite.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: rdbms-sqlite-pkg, path: './artifacts/*' }

  publish-fts-lucene:
    needs: [publish-summarizer-core, wait-for-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="dsftlucev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.FullText.Lucene/Mostlylucid.DocSummarizer.FullText.Lucene.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.FullText.Lucene/Mostlylucid.DocSummarizer.FullText.Lucene.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: fts-lucene-pkg, path: './artifacts/*' }

  publish-fts-sqlite:
    needs: [publish-summarizer-core, wait-for-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="dsftsqlv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.FullText.Sqlite/Mostlylucid.DocSummarizer.FullText.Sqlite.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.FullText.Sqlite/Mostlylucid.DocSummarizer.FullText.Sqlite.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: fts-sqlite-pkg, path: './artifacts/*' }

  publish-docsummarizer-anthropic:
    needs: [publish-summarizer-core, wait-for-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="dsanthv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.Anthropic/Mostlylucid.DocSummarizer.Anthropic.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.Anthropic/Mostlylucid.DocSummarizer.Anthropic.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: docsummarizer-anthropic-pkg, path: './artifacts/*' }

  publish-docsummarizer-openai:
    needs: [publish-summarizer-core, wait-for-docsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="dsoaiv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DocSummarizer.OpenAI/Mostlylucid.DocSummarizer.OpenAI.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DocSummarizer.OpenAI/Mostlylucid.DocSummarizer.OpenAI.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: docsummarizer-openai-pkg, path: './artifacts/*' }

  # ── Wait for backends ──────────────────────────────────────────────────
  wait-for-backends:
    needs: [publish-summarizer-core, publish-rdbms-sqlite, publish-fts-lucene, publish-fts-sqlite, publish-docsummarizer-anthropic, publish-docsummarizer-openai]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet for backend packages
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for pkg in mostlylucid.lucidrag.docsummarizer.rdbms.sqlite mostlylucid.lucidrag.docsummarizer.fulltext.lucene mostlylucid.lucidrag.docsummarizer.fulltext.sqlite mostlylucid.lucidrag.docsummarizer.anthropic mostlylucid.lucidrag.docsummarizer.openai; do
            echo "Waiting for $pkg ${VERSION}..."
            for i in $(seq 1 30); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                "https://api.nuget.org/v3-flatcontainer/$pkg/${VERSION}/$pkg.${VERSION}.nupkg")
              echo "  Attempt $i: HTTP $STATUS"
              if [ "$STATUS" = "200" ]; then break; fi
              sleep 30
            done
          done

  # ── Tier 2.5: LLM + Decomposer (parallel, after backends) ─────────────
  publish-llm:
    needs: [publish-summarizer-core, wait-for-backends]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="llmv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/LucidRAG.LLM/LucidRAG.LLM.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/LucidRAG.LLM/LucidRAG.LLM.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/LucidRAG.LLM/LucidRAG.LLM.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: llm-pkg, path: './artifacts/*' }

  publish-decomposer:
    needs: [publish-summarizer-core, wait-for-backends]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="decomv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/LucidRAG.Decomposer/LucidRAG.Decomposer.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/LucidRAG.Decomposer/LucidRAG.Decomposer.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/LucidRAG.Decomposer/LucidRAG.Decomposer.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: decomposer-pkg, path: './artifacts/*' }

  wait-for-llm-decomposer:
    needs: [publish-summarizer-core, publish-llm, publish-decomposer]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet for LLM + Decomposer
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for pkg in mostlylucid.lucidrag.llm mostlylucid.lucidrag.decomposer; do
            echo "Waiting for $pkg ${VERSION}..."
            for i in $(seq 1 30); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                "https://api.nuget.org/v3-flatcontainer/$pkg/${VERSION}/$pkg.${VERSION}.nupkg")
              echo "  Attempt $i: HTTP $STATUS"
              if [ "$STATUS" = "200" ]; then break; fi
              sleep 30
            done
          done

  # ── Tier 2.5b: ImageSummarizer + AudioSummarizer (parallel) ───────────
  publish-imagesummarizer:
    needs: [publish-summarizer-core, wait-for-docsummarizer, wait-for-storage-data]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="imgsumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/ImageSummarizer.Core/ImageSummarizer.Core.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/ImageSummarizer.Core/ImageSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/ImageSummarizer.Core/ImageSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: imagesummarizer-pkg, path: './artifacts/*' }

  publish-audiosummarizer:
    needs: [publish-summarizer-core, wait-for-docsummarizer, wait-for-storage-data]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="audiosumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/AudioSummarizer.Core/AudioSummarizer.Core.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/AudioSummarizer.Core/AudioSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/AudioSummarizer.Core/AudioSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: audiosummarizer-pkg, path: './artifacts/*' }

  # ── Wait for Image + Audio ──────────────────────────────────────────
  wait-for-image-audio:
    needs: [publish-summarizer-core, publish-imagesummarizer, publish-audiosummarizer]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet for Image + Audio
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for pkg in mostlylucid.lucidrag.imagesummarizer mostlylucid.lucidrag.audiosummarizer; do
            echo "Waiting for $pkg ${VERSION}..."
            for i in $(seq 1 30); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                "https://api.nuget.org/v3-flatcontainer/$pkg/${VERSION}/$pkg.${VERSION}.nupkg")
              echo "  Attempt $i: HTTP $STATUS"
              if [ "$STATUS" = "200" ]; then break; fi
              sleep 30
            done
          done

  # ── Tier 2.5c: VideoSummarizer (needs Image + Audio) ────────────────
  publish-videosummarizer:
    needs: [publish-summarizer-core, wait-for-image-audio]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="vidsumv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/VideoSummarizer.Core/VideoSummarizer.Core.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/VideoSummarizer.Core/VideoSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/VideoSummarizer.Core/VideoSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: videosummarizer-pkg, path: './artifacts/*' }

  # ── Tier 3: DoomSummarizer.Core ────────────────────────────────────────
  publish-doomsummarizer:
    needs: [publish-summarizer-core, wait-for-backends, wait-for-llm-decomposer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Create version tag for MinVer
        run: |
          TAG="doomcorev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true

      - name: Build & Pack
        run: |
          dotnet restore src/DoomSummarizer.Core/DoomSummarizer.Core.csproj \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/DoomSummarizer.Core/DoomSummarizer.Core.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/DoomSummarizer.Core/DoomSummarizer.Core.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true \
            -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}

      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with:
          user: 'mostlylucid'

      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          dotnet nuget push ./artifacts/*.snupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --skip-duplicate
        continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: doomsummarizer-pkg
          path: ./artifacts/*

  # ── Wait for DoomSummarizer.Core ───────────────────────────────────────
  wait-for-doomsummarizer:
    needs: [publish-summarizer-core, publish-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              "https://api.nuget.org/v3-flatcontainer/mostlylucid.lucidrag.doomsummarizer.core/${VERSION}/mostlylucid.lucidrag.doomsummarizer.core.${VERSION}.nupkg")
            echo "Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then exit 0; fi
            sleep 30
          done
          echo "Timeout — continuing anyway"

  # ── Tier 4: Sources + Readers + Plugin (all parallel) ──────────────────
  publish-source-hackernews:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srchnv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.HackerNews/Mostlylucid.DoomSummarizer.Source.HackerNews.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.HackerNews/Mostlylucid.DoomSummarizer.Source.HackerNews.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-hackernews-pkg, path: './artifacts/*' }

  publish-source-reddit:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcredditv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Reddit/Mostlylucid.DoomSummarizer.Source.Reddit.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Reddit/Mostlylucid.DoomSummarizer.Source.Reddit.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-reddit-pkg, path: './artifacts/*' }

  publish-source-google:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcgooglev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Google/Mostlylucid.DoomSummarizer.Source.Google.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Google/Mostlylucid.DoomSummarizer.Source.Google.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-google-pkg, path: './artifacts/*' }

  publish-source-news:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcnewsv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.News/Mostlylucid.DoomSummarizer.Source.News.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.News/Mostlylucid.DoomSummarizer.Source.News.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-news-pkg, path: './artifacts/*' }

  publish-source-web:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcwebv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Web/Mostlylucid.DoomSummarizer.Source.Web.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Web/Mostlylucid.DoomSummarizer.Source.Web.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-web-pkg, path: './artifacts/*' }

  publish-source-academic:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcacadv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Academic/Mostlylucid.DoomSummarizer.Source.Academic.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Academic/Mostlylucid.DoomSummarizer.Source.Academic.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-academic-pkg, path: './artifacts/*' }

  publish-source-science:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcscienv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Science/Mostlylucid.DoomSummarizer.Source.Science.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Science/Mostlylucid.DoomSummarizer.Source.Science.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-science-pkg, path: './artifacts/*' }

  publish-source-reference:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcrefv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.Reference/Mostlylucid.DoomSummarizer.Source.Reference.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.Reference/Mostlylucid.DoomSummarizer.Source.Reference.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-reference-pkg, path: './artifacts/*' }

  publish-reader-pdf:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="rdrpdfv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizers.Reader.Pdf/Mostlylucid.Summarizers.Reader.Pdf.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Summarizers.Reader.Pdf/Mostlylucid.Summarizers.Reader.Pdf.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: reader-pdf-pkg, path: './artifacts/*' }

  publish-reader-docx:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="rdrdocxv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizers.Reader.Docx/Mostlylucid.Summarizers.Reader.Docx.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Summarizers.Reader.Docx/Mostlylucid.Summarizers.Reader.Docx.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: reader-docx-pkg, path: './artifacts/*' }

  publish-reader-gutenberg:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="rdrgutv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizers.Reader.Gutenberg/Mostlylucid.Summarizers.Reader.Gutenberg.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Summarizers.Reader.Gutenberg/Mostlylucid.Summarizers.Reader.Gutenberg.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: reader-gutenberg-pkg, path: './artifacts/*' }

  publish-reader-markdown:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="rdrmdv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizers.Reader.Markdown/Mostlylucid.Summarizers.Reader.Markdown.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Summarizers.Reader.Markdown/Mostlylucid.Summarizers.Reader.Markdown.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: reader-markdown-pkg, path: './artifacts/*' }

  publish-reader-docling:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="rdrdoclv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.Summarizers.Reader.Docling/Mostlylucid.Summarizers.Reader.Docling.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.Summarizers.Reader.Docling/Mostlylucid.Summarizers.Reader.Docling.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: reader-docling-pkg, path: './artifacts/*' }

  publish-plugin-books:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgbooksv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Books/Mostlylucid.DoomSummarizer.Plugin.Books.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Books/Mostlylucid.DoomSummarizer.Plugin.Books.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-books-pkg, path: './artifacts/*' }

  publish-codesummarizer-mermaid-jint:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="codemermaidv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/CodeSummarizer.Mermaid.Jint/CodeSummarizer.Mermaid.Jint.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet build src/CodeSummarizer.Mermaid.Jint/CodeSummarizer.Mermaid.Jint.csproj \
            -c Release -p:GeneratePackageOnBuild=false \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/CodeSummarizer.Mermaid.Jint/CodeSummarizer.Mermaid.Jint.csproj \
            -c Release -o ./artifacts --no-build \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: |
          dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          dotnet nuget push ./artifacts/*.snupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: codesummarizer-mermaid-jint-pkg, path: './artifacts/*' }

  publish-source-ukgov:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcukgovv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.UkGov/Mostlylucid.DoomSummarizer.Source.UkGov.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.UkGov/Mostlylucid.DoomSummarizer.Source.UkGov.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-ukgov-pkg, path: './artifacts/*' }

  publish-source-youtube:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="srcytv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Source.YouTube/Mostlylucid.DoomSummarizer.Source.YouTube.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Source.YouTube/Mostlylucid.DoomSummarizer.Source.YouTube.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: source-youtube-pkg, path: './artifacts/*' }

  publish-plugin-audio:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgaudiov${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Audio/Mostlylucid.DoomSummarizer.Plugin.Audio.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Audio/Mostlylucid.DoomSummarizer.Plugin.Audio.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-audio-pkg, path: './artifacts/*' }

  publish-plugin-data:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgdatav${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Data/Mostlylucid.DoomSummarizer.Plugin.Data.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Data/Mostlylucid.DoomSummarizer.Plugin.Data.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-data-pkg, path: './artifacts/*' }

  publish-plugin-image:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgimagev${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Image/Mostlylucid.DoomSummarizer.Plugin.Image.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Image/Mostlylucid.DoomSummarizer.Plugin.Image.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-image-pkg, path: './artifacts/*' }

  publish-plugin-video:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgvideov${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Video/Mostlylucid.DoomSummarizer.Plugin.Video.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Video/Mostlylucid.DoomSummarizer.Plugin.Video.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-video-pkg, path: './artifacts/*' }

  publish-plugin-subtitles:
    needs: [publish-summarizer-core, wait-for-doomsummarizer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgsubv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Plugin.Subtitles/Mostlylucid.DoomSummarizer.Plugin.Subtitles.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.DoomSummarizer.Plugin.Subtitles/Mostlylucid.DoomSummarizer.Plugin.Subtitles.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugin-subtitles-pkg, path: './artifacts/*' }

  # ── Wait for all Tier 4 plugins ─────────────────────────────────────
  wait-for-tier4-plugins:
    needs:
      - publish-summarizer-core
      - publish-plugin-books
      - publish-plugin-audio
      - publish-plugin-data
      - publish-plugin-image
      - publish-plugin-video
    runs-on: ubuntu-latest
    steps:
      - name: Poll NuGet for plugins
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          for pkg in mostlylucid.lucidrag.plugins.books mostlylucid.lucidrag.plugins.audio mostlylucid.lucidrag.plugins.data mostlylucid.lucidrag.plugins.image mostlylucid.lucidrag.plugins.video; do
            echo "Waiting for $pkg ${VERSION}..."
            for i in $(seq 1 30); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                "https://api.nuget.org/v3-flatcontainer/$pkg/${VERSION}/$pkg.${VERSION}.nupkg")
              echo "  Attempt $i: HTTP $STATUS"
              if [ "$STATUS" = "200" ]; then break; fi
              sleep 30
            done
          done

  # ── Tier 4.5: Plugins.Complete (meta-package) ───────────────────────
  publish-plugins-complete:
    needs: [publish-summarizer-core, wait-for-tier4-plugins]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '${{ env.DOTNET_VERSION }}', dotnet-quality: '${{ env.DOTNET_QUALITY }}' }
      - name: Create version tag for MinVer
        run: |
          TAG="plgcompv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true
      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.LucidRAG.Plugins.Complete/Mostlylucid.LucidRAG.Plugins.Complete.csproj \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
          dotnet pack src/Mostlylucid.LucidRAG.Plugins.Complete/Mostlylucid.LucidRAG.Plugins.Complete.csproj \
            -c Release -o ./artifacts \
            -p:UseNuGetDependency=true -p:CorePackageVersion=${{ needs.publish-summarizer-core.outputs.version }}
      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with: { user: 'mostlylucid' }
      - name: Publish
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true
      - uses: actions/upload-artifact@v6
        with: { name: plugins-complete-pkg, path: './artifacts/*' }

  # ── Tier 5: Complete meta-package (fat CLI with everything) ────────────
  publish-doomsummarizer-complete:
    needs:
      - publish-summarizer-core
      - publish-codesummarizer
      - publish-storage-core
      - publish-datasummarizer-core
      - publish-imagesummarizer
      - publish-audiosummarizer
      - publish-videosummarizer
      - publish-source-hackernews
      - publish-source-reddit
      - publish-source-google
      - publish-source-news
      - publish-source-web
      - publish-source-academic
      - publish-source-science
      - publish-source-reference
      - publish-source-ukgov
      - publish-source-youtube
      - publish-reader-pdf
      - publish-reader-docx
      - publish-reader-gutenberg
      - publish-reader-markdown
      - publish-reader-docling
      - publish-plugin-books
      - publish-plugin-audio
      - publish-plugin-data
      - publish-plugin-image
      - publish-plugin-video
      - publish-plugin-subtitles
      - publish-plugins-complete
      - publish-codesummarizer-mermaid-jint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          dotnet-quality: ${{ env.DOTNET_QUALITY }}

      - name: Create version tag for MinVer
        run: |
          TAG="doomcompv${{ needs.publish-summarizer-core.outputs.version }}"
          git tag "$TAG" 2>/dev/null || true

      - name: Build & Pack
        run: |
          dotnet restore src/Mostlylucid.DoomSummarizer.Complete/Mostlylucid.DoomSummarizer.Complete.csproj
          dotnet pack src/Mostlylucid.DoomSummarizer.Complete/Mostlylucid.DoomSummarizer.Complete.csproj \
            -c Release -o ./artifacts

      - name: NuGet Login (OIDC)
        id: nuget
        uses: NuGet/login@v1
        with:
          user: 'mostlylucid'

      - name: Publish to NuGet.org
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ steps.nuget.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        continue-on-error: true

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --skip-duplicate
        continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: doomsummarizer-complete-pkg
          path: ./artifacts/*

  # ── Summary ────────────────────────────────────────────────────────────
  summary:
    runs-on: ubuntu-latest
    needs:
      - publish-summarizer-core
      - publish-codesummarizer
      - publish-storage-core
      - publish-datasummarizer-core
      - publish-docsummarizer
      - publish-rdbms-sqlite
      - publish-fts-lucene
      - publish-fts-sqlite
      - publish-docsummarizer-anthropic
      - publish-docsummarizer-openai
      - publish-llm
      - publish-decomposer
      - publish-imagesummarizer
      - publish-audiosummarizer
      - publish-videosummarizer
      - publish-doomsummarizer
      - publish-codesummarizer-mermaid-jint
      - publish-source-hackernews
      - publish-source-reddit
      - publish-source-google
      - publish-source-news
      - publish-source-web
      - publish-source-academic
      - publish-source-science
      - publish-source-reference
      - publish-source-ukgov
      - publish-source-youtube
      - publish-reader-pdf
      - publish-reader-docx
      - publish-reader-gutenberg
      - publish-reader-markdown
      - publish-reader-docling
      - publish-plugin-books
      - publish-plugin-audio
      - publish-plugin-data
      - publish-plugin-image
      - publish-plugin-video
      - publish-plugin-subtitles
      - publish-plugins-complete
      - publish-doomsummarizer-complete
    if: always()
    steps:
      - name: Summary
        run: |
          VERSION="${{ needs.publish-summarizer-core.outputs.version }}"
          echo "## Cascading NuGet Publish — v${VERSION}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tier | Package | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 0 | \`Mostlylucid.LucidRAG.Summarizer.Core\` | ${{ needs.publish-summarizer-core.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 0.5 | \`Mostlylucid.LucidRAG.CodeSummarizer\` | ${{ needs.publish-codesummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 0.5 | \`Mostlylucid.LucidRAG.Storage.Core\` | ${{ needs.publish-storage-core.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 0.5 | \`Mostlylucid.LucidRAG.DataSummarizer\` | ${{ needs.publish-datasummarizer-core.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 1 | \`Mostlylucid.LucidRAG.DocSummarizer\` | ${{ needs.publish-docsummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2 | \`Mostlylucid.LucidRAG.DocSummarizer.Rdbms.Sqlite\` | ${{ needs.publish-rdbms-sqlite.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2 | \`Mostlylucid.LucidRAG.DocSummarizer.FullText.Lucene\` | ${{ needs.publish-fts-lucene.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2 | \`Mostlylucid.LucidRAG.DocSummarizer.FullText.Sqlite\` | ${{ needs.publish-fts-sqlite.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2 | \`Mostlylucid.LucidRAG.DocSummarizer.Anthropic\` | ${{ needs.publish-docsummarizer-anthropic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2 | \`Mostlylucid.LucidRAG.DocSummarizer.OpenAI\` | ${{ needs.publish-docsummarizer-openai.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2.5 | \`Mostlylucid.LucidRAG.LLM\` | ${{ needs.publish-llm.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2.5 | \`Mostlylucid.LucidRAG.Decomposer\` | ${{ needs.publish-decomposer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2.5 | \`Mostlylucid.LucidRAG.ImageSummarizer\` | ${{ needs.publish-imagesummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2.5 | \`Mostlylucid.LucidRAG.AudioSummarizer\` | ${{ needs.publish-audiosummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 2.5 | \`Mostlylucid.LucidRAG.VideoSummarizer\` | ${{ needs.publish-videosummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 3 | \`Mostlylucid.LucidRAG.DoomSummarizer.Core\` | ${{ needs.publish-doomsummarizer.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.HackerNews\` | ${{ needs.publish-source-hackernews.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Reddit\` | ${{ needs.publish-source-reddit.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Google\` | ${{ needs.publish-source-google.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.News\` | ${{ needs.publish-source-news.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Web\` | ${{ needs.publish-source-web.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Academic\` | ${{ needs.publish-source-academic.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Science\` | ${{ needs.publish-source-science.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.Reference\` | ${{ needs.publish-source-reference.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Readers.Pdf\` | ${{ needs.publish-reader-pdf.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Readers.Docx\` | ${{ needs.publish-reader-docx.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Readers.Gutenberg\` | ${{ needs.publish-reader-gutenberg.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Readers.Markdown\` | ${{ needs.publish-reader-markdown.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Readers.Docling\` | ${{ needs.publish-reader-docling.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Sources.UkGov\` | ${{ needs.publish-source-ukgov.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.YouTube\` | ${{ needs.publish-source-youtube.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Books\` | ${{ needs.publish-plugin-books.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Audio\` | ${{ needs.publish-plugin-audio.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Data\` | ${{ needs.publish-plugin-data.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Image\` | ${{ needs.publish-plugin-image.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Video\` | ${{ needs.publish-plugin-video.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.Plugins.Subtitles\` | ${{ needs.publish-plugin-subtitles.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4 | \`Mostlylucid.LucidRAG.CodeSummarizer.Mermaid.Jint\` | ${{ needs.publish-codesummarizer-mermaid-jint.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 4.5 | \`Mostlylucid.LucidRAG.Plugins.Complete\` | ${{ needs.publish-plugins-complete.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 5 | \`Mostlylucid.LucidRAG.DoomSummarizer.Complete\` | ${{ needs.publish-doomsummarizer-complete.result }} |" >> $GITHUB_STEP_SUMMARY
