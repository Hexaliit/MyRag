Create an article outline about: "{{QUERY}}"

IMPORTANT: The article MUST focus tightly on "{{QUERY}}".
Do NOT include sections about tangential topics mentioned in the evidence.
Every section heading must directly relate to "{{QUERY}}".

EVIDENCE:
{{EVIDENCE}}

Key entities in the evidence: {{TOP_ENTITIES}}
Total evidence segments available: {{SEGMENT_COUNT}}

{{OUTLINE_INSTRUCTIONS}}

Respond with JSON only:
{% raw %}{
  "title": "compelling article title that includes key terms from query",
  "theme_description": "overall theme tightly focused on {{QUERY}}",
  "sections": [
    {
      "heading": "specific section heading directly about {{QUERY}}",
      "theme_keywords": "concrete terms from evidence that match this section AND relate to {{QUERY}}",
      "target_words": 300
    }
  ],
  "conclusion_angle": "forward-looking angle specific to {{QUERY}}",
  "key_entities": ["Entity1", "Entity2"]
}{% endraw %}

Rules:
- 4-8 sections maximum
- Section headings must DIRECTLY relate to "{{QUERY}}" — no generic headings
- AVOID generic headings like "Key Developments", "Future Directions", "Context and Background"
- PREFER specific headings like "HTMX Tag Helpers in ASP.NET Core", "Client-Side Updates Without JavaScript"
- theme_keywords should be concrete terms from the evidence, not abstract concepts
- theme_keywords must be distinct between sections — zero overlap
- If evidence about a topic is sparse, SKIP that section rather than padding with tangential content
- Do NOT include sections about topics only mentioned in passing
- key_entities lists ONLY entities that are central to "{{QUERY}}"
