# Mostlylucid.LucidRAG.Decomposer

Query decomposition utilities used for agentic retrieval (splitting complex questions into sub-queries and a search
plan).

## Install

```bash
dotnet add package Mostlylucid.LucidRAG.Decomposer
```

