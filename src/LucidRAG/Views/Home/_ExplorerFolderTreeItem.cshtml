@* Explorer Folder Tree Item - Pure Alpine.js folder tree rendering *@
@* This file is kept for backwards compatibility but the actual tree is rendered *@
@* entirely client-side via Alpine.js x-for directives in _FileExplorer.cshtml *@

@* The recursive folder tree is rendered inline in _FileExplorer.cshtml to avoid *@
@* server-side recursion issues. This partial is no longer used. *@
